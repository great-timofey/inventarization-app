// @flow

import { StyleSheet } from 'react-native';

import colors from 'global/colors';

export default StyleSheet.create({
  container: {
    width: '100%',
    height: 54,
    marginBottom: 10,
  },
  inputTitleText: {
    color: '#616161',
    fontSize: 13,
    lineHeight: 20,
  },
  input: {
    height: 30,
    width: '100%',
    color: colors.white,
    fontSize: 18,
    lineHeight: 21,
  },
});
