// @flow

export type Props = {
  region?: Object,
  latitude: number,
  longitude: number,
  latitudeDelta: number,
  customStyles?: Object,
  longitudeDelta: number,
  changeRegionCallback: Function,
};
