// @flow

export type Props = {
  region?: Object,
  latitude: number,
  longitude: number,
  showMarker: boolean,
  latitudeDelta: number,
  customStyles?: Object,
  longitudeDelta: number,
  changeRegionCallback: Function,
};
