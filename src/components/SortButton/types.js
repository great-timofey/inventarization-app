// @flow

export type Props = {
  size: number,
  iconName: string,
  iconColor?: string,
  onPress: () => void,
  customStyle?: Object,
  isDisabled?: boolean,
  isCloseButton?: boolean,
  customPosition?: Object,
};
