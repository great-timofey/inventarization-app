// @flow

export type State = {
  name: string,
  email: string,
  mobile: string,
  password: string,
  isRegForm: boolean,
  isModalVisible: boolean,
};

export type Props = {
  navigation: any,
};
